define(['react', 'lodash', './stylePanel.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'StylePanel',
        render: template
    });
});
